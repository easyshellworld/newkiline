[[redirects]]
     from = "/try/ajax/json_demo.json"
     to = "https://www.runoob.com/try/ajax/json_demo.json"
     status = 200
     force = true # COMMENT: ensure that we always redirect
     headers = {X-From = "Netlify"}

[[headers]]
    for="/*"
    [headers.values]
    Acces-Control-Allow-Origin="*"
